{
  "name": "addressnormalizationservice",
  "version": "1.0.0",
  "description": "Just an example of an http server in Node with an odd project name.",
  "main": "addressnormalizerservice.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
